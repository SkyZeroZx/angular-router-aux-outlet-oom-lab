import { Component } from "@angular/core";

// Deliberately trivial. The cost measured here is paid before this renders.
@Component({
  selector: "app-shop-page",
  template: '<main id="shop-page">Shop</main>',
})
export class ShopPage {}
